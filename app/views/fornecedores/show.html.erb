<p style="color: green"><%= notice %></p>

<%= render @fornecedor %>

<div>
  <%= link_to "Editar fornecedor", edit_fornecedor_path(@fornecedor), class: "btn btn-primary" %> |
  <%= link_to "Voltar", fornecedores_path, class: "btn btn-primary" %> |

  <%= button_to "Apagar", @fornecedor, method: :delete, class: "btn btn-danger", form: { style: "display: inline", onsubmit:"return confirm('Confirma?')" }%>
</div>
